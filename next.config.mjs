/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  output: 'export',
  trailingSlash: true,
  basePath: '/portfolio',
  reactCompiler: true,
};

export default nextConfig;
